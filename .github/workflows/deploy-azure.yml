name: Build and Push Medusa Container

on:
  push:
    branches:
      - radeon
    paths:
      - 'backend/**'
      - 'store-userview/**'
      - 'Dockerfile'
      - 'nginx.conf'
      - 'start.sh'
      - '.github/workflows/deploy-azure.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push container image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/radeon:latest

      - name: Trigger Azure Deployment Webhook
        env:
          AZURE_WEBHOOK_URL: ${{ secrets.AZURE_WEBHOOK_URL }}
          DEFAULT_WEBHOOK: "https://$radeon:YqmcEzagwxo2pZPbJunB8AsHYx3W2SaC9ZKDu8LWXwriCcYrrpzcshfxDeS4@radeon-hphbfha8emfthkbf.scm.eastasia-01.azurewebsites.net/api/registry/webhook"
        run: |
          WEBHOOK="${AZURE_WEBHOOK_URL:-$DEFAULT_WEBHOOK}"
          echo "Triggering Azure App Service webhook..."
          curl -s -i -X POST "$WEBHOOK"
          echo "Azure webhook triggered successfully."
